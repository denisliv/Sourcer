server {
    listen 3000;
    server_name _;

    root /usr/share/nginx/html;

    # Static assets with aggressive caching
    location /static/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Proxy API and auth requests to backend
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Health check pass-through
    location /health {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
    }

    # Favicon
    location /favicon.ico {
        rewrite ^ /static/favicon.svg permanent;
    }

    # Explicit HTML page routes (must match request paths to correct files)
    location = /login {
        try_files /login.html =404;
    }
    location = /account {
        try_files /account.html =404;
    }
    location = /admin/users {
        try_files /admin_users.html =404;
    }

    # SPA fallback and root
    location / {
        try_files $uri $uri/ /index.html;
    }
}
